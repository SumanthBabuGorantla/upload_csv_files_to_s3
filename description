In the above code we will upload a csv file from local to s3 by providing the credentials of the s3 bucket automatically by running the code once.
If the new files are added to the location and run the code, it will add the files in a incremental load format.
